{
  "name": "theplanets",
  "version": "1.0.0",
  "description": "Angular 2 application displaying information about our solar system.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-prebuild": "echo This runs before Heroku installs your dependencies.",
    "heroku-postbuild": "npm install --prefix ./client",
    "concurrent-tasks": "concurrently --kill-others"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.13.3",
    "concurrently": "^3.1.0",
    "ejs": "^2.5.5",
    "express": "^4.13.3",
    "zone.js": "^0.7.0",
    "angular-cli": "1.0.0-beta.24",
    "rxjs": "5.0.1"
  },
  "engines": {
    "node": "4.1.1"
  }
}
