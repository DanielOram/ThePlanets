{
  "name": "theplanets",
  "version": "1.0.0",
  "description": "Angular 2 application displaying information about our solar system.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-prebuild": "echo This runs before Heroku installs your dependencies.",
    "heroku-postbuild": "npm install --prefix ./client && cd client && npm run build:prod && cd ..",
    "start:angular": "cd client && npm start",
    "build:prod": "ng build -prod",
    "concurrent-tasks": "concurrently --kill-others"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.13.3",
    "concurrently": "^3.1.0",
    "express": "^4.13.3"
  },
  "engines": {
    "node": "4.1.1"
  }
}
